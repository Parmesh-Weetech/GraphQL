# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddToCartInput {
  productId: ID!
  quantity: String!
}

type Cart {
  cartItems: [CartItem!]!
  totalAmount: Float!
  user: User!
}

type CartItem {
  cart: Cart!
  product: Product!
  quantity: Int!
  total: Float!
}

input CreateOrderInput {
  userId: ID!
}

input CreateProductInput {
  name: String!
  price: String!
}

input CreateUserInput {
  email: String!
  name: String!
  password: String!
  userRole: String!
}

input FindOrdersByUserInput {
  userId: ID!
}

type Mutation {
  addToCart(input: AddToCartInput!, userId: ID!): Cart
  clearCart(userId: ID!): Cart
  createOrderFromCart(input: CreateOrderInput!): Order
  createProduct(input: CreateProductInput!): Product
  createUser(input: CreateUserInput!): User
  deleteProduct(id: ID!): Boolean!
  deleteUser(id: ID!): Boolean!
  removeFromCart(cartItemId: ID!, userId: ID!): Cart
  updateCartItem(input: UpdateCartItemInput!, userId: ID!): Cart
  updateOrderStatus(input: UpdateOrderStatusInput!): Order
  updateProduct(id: ID!, input: UpdateProductInput!): Product
  updateUser(id: ID!, input: UpdateUserInput!): User
}

type Order {
  orderItems: [OrderItem!]!
  status: OrderStatus!
  totalAmount: Float!
  user: User!
}

type OrderItem {
  order: Order!
  product: Product!
  quantity: Int!
  total: Float!
}

enum OrderStatus {
  CANCALLED
  DELIVERED
  FAIL
  ORDERED
  PAYMENT_PENDING
  PENDING
  SHIPPED
}

type Product {
  name: String!
  price: Float!
}

type Query {
  findCartByUserId(userId: ID!): Cart
  findOrderById(id: ID!): Order
  findOrdersByUserId(input: FindOrdersByUserInput!): [Order!]!
  findProductById(id: ID!): Product
  findUserById(id: ID!): User
  listOrders: [Order!]!
  listProducts: [Product!]!
  listUsers: [User!]!
}

input UpdateCartItemInput {
  cartItemId: ID!
  quantity: String!
}

input UpdateOrderStatusInput {
  orderId: ID!
  status: OrderStatus!
}

input UpdateProductInput {
  name: String
  price: String
}

input UpdateUserInput {
  email: String
  name: String
  password: String
  userRole: String
}

type User {
  email: String!
  name: String!
  password: String!
  salt: String!
  userRole: String!
}